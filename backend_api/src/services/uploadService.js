const db = require('../../db');
const fs = require('fs');

/**
 * upload files in upload/projects/
 * @param files
 * @returns {Promise<void>}
 */
const upload = async files => {
  try {
  } catch (e) {
    console.log(e);
  }
};

module.exports = {
  upload
};
